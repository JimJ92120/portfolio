type Config = {
  // tileSize: [number, number];
  maxSceneSize: number;
  frameSize: [number, number];
  typeSpeed: number;
};

export default {
  // tileSize: [50, 50],
  maxSceneSize: 500,
  frameSize: [10, 10],
  typeSpeed: 50,
} as Config;
