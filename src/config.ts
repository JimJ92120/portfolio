type Config = {
  // tileSize: [number, number];
  maxSceneSize: number;
  frameSize: [number, number];
};

export default {
  // tileSize: [50, 50],
  maxSceneSize: 500,
  frameSize: [10, 10],
} as Config;
